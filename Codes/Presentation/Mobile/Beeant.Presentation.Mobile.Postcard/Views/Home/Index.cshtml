 @{
     Layout = "";
 }
@using Beeant.Basic.Services.Mvc.Extension
@model Beeant.Domain.Entities.Cms.PostcardEntity
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width"/>
    <link rel="shortcut icon" href="@Html.GetUrl("PresentationMobileSharedUrl")/images/favicon.ico" type="image/x-icon" />
    <title>@if (Model != null)
           {
               @Model.Name;
           }</title>
   
   
</head>
<body>
    @if (Model != null)
    {
        @(new HtmlString(Model.Detail).ToHtmlString());
    }
</body>
</html>
